{
  "name": "currency-exchange-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --production=false --prefix client && npm run build --prefix client && npm install --production=false --prefix server && npm run build --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}